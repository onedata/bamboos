[{kernel, [{logger, [
    % By default logs from system nodes are forwarded to test node (this is done by cth_log_redirect).
    % Disabling default handler on test node results in those forwaded logs not being logged on console.
    {handler, default, undefined} 
]}]}].

